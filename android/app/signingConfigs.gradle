android {
    signingConfigs {
        release {
            keyAlias = "key"
            keyPassword = "android123"
            storeFile = file("key.jks")
            storePassword = "android123"
        }
    }
}